<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>좋아요한 곡 - Haneum</title>
    <link rel="apple-touch-icon" sizes="180x180" href="/favicon/apple-touch-icon.png">
    <link rel="icon" type="image/png" sizes="32x32" href="/favicon/favicon-32x32.png">
    <link rel="icon" type="image/png" sizes="16x16" href="/favicon/favicon-16x16.png">
    <link rel="manifest" href="/favicon/site.webmanifest">
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css">
</head>
<body class="bg-[#f8f9fa] text-gray-900 min-h-screen">
    <%- include('partials/header') %>

    <main class="container mx-auto max-w-5xl px-4 py-8 md:py-12">
        <!-- Header -->
        <div class="mb-12">
            <nav class="flex items-center gap-2 text-sm text-gray-500 mb-6">
                <a href="/" class="hover:text-blue-600 transition-colors">홈</a>
                <i class="fas fa-chevron-right text-[10px]"></i>
                <a href="/me" class="hover:text-blue-600 transition-colors">내 학습 현황</a>
                <i class="fas fa-chevron-right text-[10px]"></i>
                <span class="text-gray-900 font-medium">좋아요한 곡</span>
            </nav>

            <div class="bg-white rounded-[2rem] p-8 md:p-12 shadow-sm border border-gray-100 relative overflow-hidden group">
                <div class="absolute -top-12 -right-12 w-64 h-64 bg-pink-50/50 rounded-full blur-3xl group-hover:bg-pink-100/50 transition-colors duration-500"></div>
                
                <div class="relative z-10 flex flex-col md:flex-row md:items-center gap-8">
                    <div class="w-24 h-24 md:w-32 md:h-32 bg-gradient-to-br from-pink-500 to-rose-500 text-white rounded-[2rem] flex items-center justify-center text-4xl shadow-xl shadow-pink-200">
                        <i class="fas fa-heart"></i>
                    </div>
                    
                    <div class="flex-grow">
                        <h1 class="text-3xl md:text-5xl font-black text-gray-900 tracking-tight mb-2">좋아요한 곡</h1>
                        <p class="text-gray-500 text-lg font-medium leading-relaxed">
                            당신이 마음을 표현한 <span class="text-pink-500 font-bold"><%= totalLikes %></span>곡의 노래들입니다.
                        </p>
                    </div>
                </div>
            </div>
        </div>

        <!-- Likes Grid -->
        <section class="space-y-6">
            <% if (likes.length === 0) { %>
                <div class="bg-white rounded-[2rem] p-16 text-center border-2 border-dashed border-gray-100">
                    <div class="w-16 h-16 bg-gray-50 text-gray-300 rounded-2xl flex items-center justify-center text-2xl mx-auto mb-4">
                        <i class="fas fa-heart-broken"></i>
                    </div>
                    <p class="text-gray-500 font-medium">아직 좋아요한 곡이 없습니다.</p>
                    <a href="/" class="mt-6 inline-flex items-center gap-2 px-6 py-3 bg-blue-600 text-white rounded-xl font-bold hover:bg-blue-700 transition-all shadow-lg shadow-blue-200">
                        노래 찾아보기
                    </a>
                </div>
            <% } else { %>
                <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                    <% likes.forEach((item) => { %>
                        <div class="group bg-white rounded-[2rem] shadow-sm hover:shadow-xl hover:shadow-pink-500/5 border border-gray-100 transition-all duration-300 transform hover:-translate-y-1 overflow-hidden">
                            <div class="p-6 md:p-8 flex items-center gap-6">
                                <!-- Song Info -->
                                <div class="flex-grow min-w-0">
                                    <a href="/songs/<%= item.song.name %>" class="block group/song">
                                        <div class="space-y-1 mb-1">
                                            <p class="text-sm text-pink-500 font-bold">
                                                <%= item.song.kor_name || item.song.ori_name || '제목 정보 없음' %>
                                            </p>
                                        </div>

                                        <div class="flex items-start justify-between mb-3">
                                            <div class="flex-1">
                                                <h3 class="font-black text-xl text-gray-800 group-hover/song:text-pink-600 transition-colors line-clamp-1 leading-tight">
                                                    <%= item.song.ori_name || item.song.name %>
                                                </h3>
                                            </div>
                                        </div>
                                    </a>
                                    
                                    <div class="flex flex-wrap items-center gap-4 mt-2">
                                        <!-- Artist -->
                                        <div class="flex items-center space-x-2">
                                            <div class="w-6 h-6 rounded-full bg-pink-50 text-pink-500 flex items-center justify-center shadow-sm">
                                                <i class="fas fa-music text-[10px]"></i>
                                            </div>
                                            <% 
                                            let sArtistName = '아티스트 정보 없음';
                                            let sArtistQuery = '';
                                            if (item.song.artist) {
                                                if (typeof item.song.artist === 'object') {
                                                    sArtistName = item.song.artist.kor_name || item.song.artist.eng_name || item.song.artist.ori_name || '아티스트 정보 없음';
                                                    sArtistQuery = item.song.artist.ori_name || item.song.artist.kor_name;
                                                } else {
                                                    sArtistName = item.song.artist;
                                                    sArtistQuery = item.song.artist;
                                                }
                                            }
                                            %>
                                            <a href="/songs?q=<%= encodeURIComponent(sArtistQuery) %>" class="text-sm text-gray-600 font-bold hover:text-pink-600 transition-colors cursor-pointer">
                                                <%= sArtistName %>
                                            </a>
                                        </div>

                                        <!-- Date -->
                                        <div class="flex items-center space-x-1.5 text-[10px] font-black text-gray-400 uppercase tracking-widest">
                                            <i class="fas fa-clock"></i>
                                            <span>
                                                <%= new Date(item.created_at).toLocaleString('ko-KR', { month: 'long', day: 'numeric', hour: '2-digit', minute: '2-digit' }) %> 좋아요
                                            </span>
                                        </div>
                                    </div>
                                </div>
                                
                                <!-- Action -->
                                <div class="flex-shrink-0">
                                    <a href="/view/<%= item.song.name %>" class="w-14 h-14 rounded-2xl bg-pink-50 text-pink-500 flex items-center justify-center hover:bg-pink-500 hover:text-white transition-all shadow-sm group/btn">
                                        <i class="fas fa-play text-lg group-hover/btn:scale-110 transition-transform"></i>
                                    </a>
                                </div>
                            </div>
                        </div>
                    <% }) %>
                </div>

                <!-- 페이지네이션 -->
                <% if (totalPages > 1) { %>
                    <div class="mt-16 flex justify-center items-center gap-2">
                        <% if (currentPage > 1) { %>
                            <a href="/me/likes?page=1" 
                               class="px-4 py-2 bg-white border border-gray-100 rounded-xl text-sm font-bold text-gray-500 hover:border-pink-300 hover:text-pink-600 transition-all shadow-sm">
                                처음
                            </a>
                        <% } %>
                        
                        <% 
                        let startPage = Math.max(1, currentPage - 2);
                        let endPage = Math.min(totalPages, startPage + 4);
                        if (endPage - startPage < 4) startPage = Math.max(1, endPage - 4);
                        
                        for (let i = startPage; i <= endPage; i++) { 
                            if (i < 1) continue;
                        %>
                            <a href="/me/likes?page=<%= i %>" 
                               class="w-10 h-10 flex items-center justify-center rounded-xl border transition-all font-bold <%= i === currentPage ? 'bg-pink-600 border-pink-600 text-white shadow-lg shadow-pink-200' : 'bg-white border-gray-100 text-gray-500 hover:border-pink-300 hover:text-pink-600' %>">
                                <%= i %>
                            </a>
                        <% } %>

                        <% if (currentPage < totalPages) { %>
                            <a href="/me/likes?page=<%= totalPages %>" 
                               class="px-4 py-2 bg-white border border-gray-100 rounded-xl text-sm font-bold text-gray-500 hover:border-pink-300 hover:text-pink-600 transition-all shadow-sm">
                                마지막
                            </a>
                        <% } %>
                    </div>
                <% } %>
            <% } %>
        </section>
    </main>
    <%- include('partials/footer') %>
</body>
</html>

