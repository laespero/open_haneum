<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>태그 일괄 관리 - 오픈 한음</title>
    <link rel="apple-touch-icon" sizes="180x180" href="/favicon/apple-touch-icon.png">
    <link rel="icon" type="image/png" sizes="32x32" href="/favicon/favicon-32x32.png">
    <link rel="icon" type="image/png" sizes="16x16" href="/favicon/favicon-16x16.png">
    <link rel="manifest" href="/favicon/site.webmanifest">
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css">
</head>
<body class="bg-gray-100 text-gray-800 font-sans leading-relaxed">
    <%- include('partials/header') %>
    <!-- 페이지 타이틀 -->
    <div class="bg-teal-600 text-white text-center py-8 px-6">
        <div class="container mx-auto max-w-6xl">
            <h1 class="text-3xl md:text-4xl font-bold mb-4">태그 일괄 관리</h1>
            <p class="text-teal-100">특정 태그를 다른 태그로 일괄 변경합니다.</p>
            
            <div class="flex justify-center gap-4 mt-6">
                <a href="/" class="bg-white text-teal-600 font-semibold px-6 py-3 rounded-full transition duration-300 ease-in-out inline-flex items-center justify-center gap-2">
                    <i class="fas fa-home"></i> 메인으로
                </a>
                <a href="/admin" class="bg-teal-500 hover:bg-teal-400 text-white font-semibold px-6 py-3 rounded-full transition duration-300 ease-in-out inline-flex items-center justify-center gap-2">
                    <i class="fas fa-cog"></i> 관리자 페이지
                </a>
            </div>
        </div>
    </div>

    <main class="py-8 px-6">
        <div class="container mx-auto max-w-6xl">
            <div class="grid grid-cols-1 lg:grid-cols-3 gap-8">
                <!-- 왼쪽: 태그 변경 폼 -->
                <div class="lg:col-span-1">
                    <div class="bg-white rounded-lg shadow-sm p-6 sticky top-8">
                        <h2 class="text-xl font-semibold mb-4 border-b pb-2 text-teal-800">
                            <i class="fas fa-exchange-alt mr-2"></i> 태그 변경
                        </h2>
                        
                        <div class="space-y-4">
                            <div>
                                <label class="block text-sm font-medium text-gray-700 mb-1">변경 전 태그 (대상)</label>
                                <input type="text" id="oldTagInput" class="w-full px-4 py-2 border rounded-lg focus:ring-2 focus:ring-teal-500 focus:border-teal-500 outline-none" placeholder="예: UtatenImport">
                                <p class="text-xs text-gray-500 mt-1">오른쪽 목록에서 클릭하면 자동 입력됩니다.</p>
                            </div>
                            
                            <div class="flex justify-center">
                                <i class="fas fa-arrow-down text-gray-400 text-xl"></i>
                            </div>

                            <div>
                                <label class="block text-sm font-medium text-gray-700 mb-1">변경 후 태그 (새 이름)</label>
                                <input type="text" id="newTagInput" class="w-full px-4 py-2 border rounded-lg focus:ring-2 focus:ring-teal-500 focus:border-teal-500 outline-none" placeholder="예: 일본어">
                            </div>
                            
                            <button onclick="replaceTag()" class="w-full bg-teal-600 hover:bg-teal-700 text-white font-bold py-3 rounded-lg transition duration-300 shadow-md">
                                일괄 변경 실행
                            </button>
                        </div>

                        <div class="mt-6 bg-blue-50 p-4 rounded-lg text-sm text-blue-800">
                            <h4 class="font-bold mb-2"><i class="fas fa-info-circle"></i> 주의사항</h4>
                            <ul class="list-disc list-inside space-y-1">
                                <li>변경 작업은 되돌릴 수 없습니다.</li>
                                <li>모든 노래 파일(.json)이 수정됩니다.</li>
                                <li>변경 후 태그가 이미 존재하는 경우 중복되지 않게 합쳐집니다.</li>
                            </ul>
                        </div>
                    </div>
                </div>

                <!-- 오른쪽: 현재 태그 현황 -->
                <div class="lg:col-span-2">
                    <div class="bg-white rounded-lg shadow-sm p-6">
                        <div class="flex justify-between items-center mb-4 border-b pb-2">
                            <h2 class="text-xl font-semibold text-gray-800">
                                <i class="fas fa-tags mr-2"></i> 태그 현황
                            </h2>
                            <span class="bg-gray-100 text-gray-600 px-3 py-1 rounded-full text-sm font-medium">
                                총 <%= tags.length %>종류
                            </span>
                        </div>
                        
                        <% if (tags.length === 0) { %>
                            <div class="text-center py-12 text-gray-500">
                                <i class="fas fa-tag text-4xl mb-3 opacity-30"></i>
                                <p>등록된 태그가 없습니다.</p>
                            </div>
                        <% } else { %>
                            <div class="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-3">
                                <% tags.forEach(item => { %>
                                    <button onclick="selectTag('<%= item.tag %>')" 
                                            class="group flex justify-between items-center bg-gray-50 hover:bg-teal-50 border hover:border-teal-200 rounded-lg p-3 transition duration-200 text-left">
                                        <span class="font-medium text-gray-700 group-hover:text-teal-700 truncate mr-2" title="<%= item.tag %>">
                                            <%= item.tag %>
                                        </span>
                                        <span class="bg-white text-gray-500 text-xs font-bold px-2 py-1 rounded-full shadow-sm min-w-[2rem] text-center group-hover:text-teal-600">
                                            <%= item.count %>
                                        </span>
                                    </button>
                                <% }); %>
                            </div>
                        <% } %>
                    </div>
                </div>
            </div>
        </div>
    </main>

    <script>
        function selectTag(tagName) {
            document.getElementById('oldTagInput').value = tagName;
            // 입력창 강조 효과
            const input = document.getElementById('oldTagInput');
            input.classList.add('ring-2', 'ring-teal-500');
            setTimeout(() => input.classList.remove('ring-2', 'ring-teal-500'), 300);
        }

        async function replaceTag() {
            const oldTag = document.getElementById('oldTagInput').value.trim();
            const newTag = document.getElementById('newTagInput').value.trim();

            if (!oldTag || !newTag) {
                alert('변경 전 태그와 변경 후 태그를 모두 입력해주세요.');
                return;
            }

            if (oldTag === newTag) {
                alert('변경 전과 후의 태그가 같습니다.');
                return;
            }

            if (!confirm(`정말로 모든 노래에서 "${oldTag}" 태그를 "${newTag}"(으)로 변경하시겠습니까?\n이 작업은 되돌릴 수 없습니다.`)) {
                return;
            }

            // 로딩 표시
            const btn = event.currentTarget;
            const originalText = btn.innerHTML;
            btn.disabled = true;
            btn.innerHTML = '<i class="fas fa-spinner fa-spin"></i> 처리 중...';

            try {
                const response = await fetch('/admin/tags/replace', {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json',
                    },
                    body: JSON.stringify({ oldTag, newTag })
                });

                const result = await response.json();

                if (result.success) {
                    alert(result.message);
                    window.location.reload();
                } else {
                    alert('오류: ' + result.message);
                }
            } catch (error) {
                console.error(error);
                alert('서버 통신 중 오류가 발생했습니다.');
            } finally {
                btn.disabled = false;
                btn.innerHTML = originalText;
            }
        }
    </script>
</body>
</html>


