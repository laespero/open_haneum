<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>아티스트 일괄 관리 - 오픈 한음</title>
    <link rel="apple-touch-icon" sizes="180x180" href="/favicon/apple-touch-icon.png">
    <link rel="icon" type="image/png" sizes="32x32" href="/favicon/favicon-32x32.png">
    <link rel="icon" type="image/png" sizes="16x16" href="/favicon/favicon-16x16.png">
    <link rel="manifest" href="/favicon/site.webmanifest">
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css">
</head>
<body class="bg-gray-100 text-gray-800 font-sans leading-relaxed">
    <!-- 헤더 -->
    <header class="bg-purple-600 text-white text-center py-8 px-6">
        <div class="container mx-auto max-w-6xl">
            <h1 class="text-3xl md:text-4xl font-bold mb-4">아티스트 일괄 관리</h1>
            <p class="text-purple-100">아티스트의 이름 정보를 일괄 수정합니다.</p>
            
            <div class="flex justify-center gap-4 mt-6">
                <a href="/" class="bg-white text-purple-600 font-semibold px-6 py-3 rounded-full transition duration-300 ease-in-out inline-flex items-center justify-center gap-2">
                    <i class="fas fa-home"></i> 메인으로
                </a>
                <a href="/admin" class="bg-purple-500 hover:bg-purple-400 text-white font-semibold px-6 py-3 rounded-full transition duration-300 ease-in-out inline-flex items-center justify-center gap-2">
                    <i class="fas fa-cog"></i> 관리자 페이지
                </a>
            </div>
        </div>
    </header>

    <main class="py-8 px-6">
        <div class="container mx-auto max-w-6xl">
            <div class="grid grid-cols-1 lg:grid-cols-3 gap-8">
                <!-- 왼쪽: 수정 폼 -->
                <div class="lg:col-span-1">
                    <div class="bg-white rounded-lg shadow-sm p-6 sticky top-8">
                        <h2 class="text-xl font-semibold mb-4 border-b pb-2 text-purple-800">
                            <i class="fas fa-edit mr-2"></i> 정보 수정
                        </h2>
                        
                        <div class="space-y-4">
                            <div>
                                <label class="block text-sm font-medium text-gray-700 mb-1">대상 아티스트 (원어명)</label>
                                <input type="text" id="targetOriName" readonly class="w-full px-4 py-2 border rounded-lg bg-gray-100 text-gray-500 cursor-not-allowed outline-none" placeholder="오른쪽 목록에서 선택하세요">
                                <p class="text-xs text-gray-500 mt-1">목록에서 선택하면 자동 입력됩니다.</p>
                            </div>
                            
                            <hr class="border-gray-200 my-2">

                            <div>
                                <label class="block text-sm font-medium text-gray-700 mb-1">한국어 이름</label>
                                <input type="text" id="newKorName" class="w-full px-4 py-2 border rounded-lg focus:ring-2 focus:ring-purple-500 focus:border-purple-500 outline-none" placeholder="예: 스피츠">
                            </div>

                            <div>
                                <label class="block text-sm font-medium text-gray-700 mb-1">영어 이름</label>
                                <input type="text" id="newEngName" class="w-full px-4 py-2 border rounded-lg focus:ring-2 focus:ring-purple-500 focus:border-purple-500 outline-none" placeholder="예: Spitz">
                            </div>
                            
                            <button onclick="updateArtist()" id="submitBtn" disabled class="w-full bg-purple-600 hover:bg-purple-700 disabled:bg-gray-400 disabled:cursor-not-allowed text-white font-bold py-3 rounded-lg transition duration-300 shadow-md mt-4">
                                일괄 수정 실행
                            </button>
                        </div>

                        <div class="mt-6 bg-blue-50 p-4 rounded-lg text-sm text-blue-800">
                            <h4 class="font-bold mb-2"><i class="fas fa-info-circle"></i> 안내</h4>
                            <ul class="list-disc list-inside space-y-1">
                                <li>선택한 아티스트(원어명)와 일치하는 <strong>모든 노래</strong>의 아티스트 정보가 업데이트됩니다.</li>
                                <li>기존에 문자열로만 저장된 아티스트 정보도 객체 형태(원어/한글/영어)로 변환됩니다.</li>
                            </ul>
                        </div>
                    </div>
                </div>

                <!-- 오른쪽: 아티스트 목록 -->
                <div class="lg:col-span-2">
                    <div class="bg-white rounded-lg shadow-sm p-6">
                        <div class="flex flex-col sm:flex-row justify-between items-center mb-4 border-b pb-4 gap-4">
                            <div class="flex items-center gap-3">
                                <h2 class="text-xl font-semibold text-gray-800">
                                    <i class="fas fa-users mr-2"></i> 아티스트 목록
                                </h2>
                                <span class="bg-gray-100 text-gray-600 px-3 py-1 rounded-full text-sm font-medium">
                                    총 <%= artists.length %>명
                                </span>
                            </div>
                            
                            <!-- 검색창 -->
                            <div class="relative w-full sm:w-64">
                                <input type="text" id="searchInput" onkeyup="filterArtists()" placeholder="아티스트 검색..." class="w-full pl-10 pr-4 py-2 border rounded-lg focus:ring-2 focus:ring-purple-500 outline-none">
                                <i class="fas fa-search absolute left-3 top-3 text-gray-400"></i>
                            </div>
                        </div>
                        
                        <div class="overflow-y-auto max-h-[800px] pr-2 custom-scrollbar">
                            <% if (artists.length === 0) { %>
                                <div class="text-center py-12 text-gray-500">
                                    <p>등록된 아티스트가 없습니다.</p>
                                </div>
                            <% } else { %>
                                <div class="grid grid-cols-1 md:grid-cols-2 gap-3" id="artistGrid">
                                    <% artists.forEach(artist => { %>
                                        <button onclick="selectArtist('<%= artist.ori_name %>', '<%= artist.kor_name %>', '<%= artist.eng_name %>')" 
                                                class="artist-card group flex flex-col items-start bg-gray-50 hover:bg-purple-50 border hover:border-purple-200 rounded-lg p-4 transition duration-200 text-left w-full"
                                                data-search="<%= (artist.ori_name + ' ' + artist.kor_name + ' ' + artist.eng_name).toLowerCase() %>">
                                            <div class="flex justify-between w-full mb-1">
                                                <span class="font-bold text-gray-800 group-hover:text-purple-800 text-lg truncate" title="<%= artist.ori_name %>">
                                                    <%= artist.ori_name %>
                                                </span>
                                                <span class="bg-white text-gray-500 text-xs font-bold px-2 py-1 rounded-full shadow-sm h-6 min-w-[2rem] flex items-center justify-center group-hover:text-purple-600">
                                                    <%= artist.count %>곡
                                                </span>
                                            </div>
                                            <div class="text-sm text-gray-600 w-full truncate">
                                                <% if (artist.kor_name) { %>
                                                    <span class="mr-2"><i class="fas fa-language text-xs text-gray-400 mr-1"></i><%= artist.kor_name %></span>
                                                <% } else { %>
                                                    <span class="mr-2 text-gray-400"><i class="fas fa-language text-xs text-gray-300 mr-1"></i>한글명 없음</span>
                                                <% } %>
                                            </div>
                                            <div class="text-sm text-gray-600 w-full truncate">
                                                <% if (artist.eng_name) { %>
                                                    <span><i class="fas fa-globe text-xs text-gray-400 mr-1"></i><%= artist.eng_name %></span>
                                                <% } else { %>
                                                    <span class="text-gray-400"><i class="fas fa-globe text-xs text-gray-300 mr-1"></i>영문명 없음</span>
                                                <% } %>
                                            </div>
                                        </button>
                                    <% }); %>
                                </div>
                            <% } %>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </main>

    <script>
        function selectArtist(oriName, korName, engName) {
            document.getElementById('targetOriName').value = oriName;
            document.getElementById('newKorName').value = korName;
            document.getElementById('newEngName').value = engName;
            
            document.getElementById('submitBtn').disabled = false;
            
            // 입력창 강조 및 스크롤
            const formCard = document.querySelector('.sticky');
            formCard.classList.add('ring-2', 'ring-purple-500');
            setTimeout(() => formCard.classList.remove('ring-2', 'ring-purple-500'), 500);
            
            // 모바일에서는 폼으로 스크롤
            if (window.innerWidth < 1024) {
                window.scrollTo({ top: 0, behavior: 'smooth' });
            }
        }

        function filterArtists() {
            const input = document.getElementById('searchInput');
            const filter = input.value.toLowerCase();
            const cards = document.getElementsByClassName('artist-card');

            for (let i = 0; i < cards.length; i++) {
                const searchData = cards[i].getAttribute('data-search');
                if (searchData.includes(filter)) {
                    cards[i].style.display = "";
                } else {
                    cards[i].style.display = "none";
                }
            }
        }

        async function updateArtist() {
            const targetOriName = document.getElementById('targetOriName').value;
            const newKorName = document.getElementById('newKorName').value.trim();
            const newEngName = document.getElementById('newEngName').value.trim();

            if (!targetOriName) {
                alert('대상 아티스트가 선택되지 않았습니다.');
                return;
            }

            if (!confirm(`"${targetOriName}" 아티스트의 정보를 일괄 수정하시겠습니까?\n\n한국어: ${newKorName || '(없음)'}\n영어: ${newEngName || '(없음)'}`)) {
                return;
            }

            // 로딩 표시
            const btn = document.getElementById('submitBtn');
            const originalText = btn.innerHTML;
            btn.disabled = true;
            btn.innerHTML = '<i class="fas fa-spinner fa-spin"></i> 처리 중...';

            try {
                const response = await fetch('/admin/artists/update', {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json',
                    },
                    body: JSON.stringify({ targetOriName, newKorName, newEngName })
                });

                const result = await response.json();

                if (result.success) {
                    alert(result.message);
                    window.location.reload();
                } else {
                    alert('오류: ' + result.message);
                }
            } catch (error) {
                console.error(error);
                alert('서버 통신 중 오류가 발생했습니다.');
            } finally {
                btn.disabled = false;
                btn.innerHTML = originalText;
            }
        }
    </script>
    
    <style>
        /* 스크롤바 스타일링 */
        .custom-scrollbar::-webkit-scrollbar {
            width: 8px;
        }
        .custom-scrollbar::-webkit-scrollbar-track {
            background: #f1f1f1;
            border-radius: 4px;
        }
        .custom-scrollbar::-webkit-scrollbar-thumb {
            background: #d8b4fe;
            border-radius: 4px;
        }
        .custom-scrollbar::-webkit-scrollbar-thumb:hover {
            background: #c084fc;
        }
    </style>
</body>
</html>


