<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
    <% if (searchQuery && searchQuery.trim()) { %>
        "<%= searchQuery %>" 검색 결과
    <% } else { %>
        전체 노래 목록
    <% } %> - Haneum</title>
    <link rel="apple-touch-icon" sizes="180x180" href="/favicon/apple-touch-icon.png">
    <link rel="icon" type="image/png" sizes="32x32" href="/favicon/favicon-32x32.png">
    <link rel="icon" type="image/png" sizes="16x16" href="/favicon/favicon-16x16.png">
    <link rel="manifest" href="/favicon/site.webmanifest">
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css">
</head>
<body class="bg-gray-100 text-gray-800 font-sans leading-relaxed flex flex-col min-h-screen">
    <!-- 헤더 -->
    <header class="bg-white shadow-sm sticky top-0 z-50">
        <div class="container mx-auto max-w-4xl px-5 py-3 flex items-center gap-4">
            <a href="/" class=" px-2 text-2xl font-bold text-blue-600 flex-shrink-0">Haneum</a>
            <form action="/songs" method="get" class="flex-grow min-w-0" id="searchForm">
                <div class="flex px-2">
                    <input type="search" name="q" id="searchInput" value="<%= searchQuery %>" placeholder="노래 검색..." 
                           class="flex-grow px-4 py-2 border border-gray-300 rounded-l-full focus:outline-none focus:ring-2 focus:ring-blue-500 min-w-0">
                    <button type="submit" class="bg-blue-600 text-white px-4 sm:px-6 py-2 rounded-r-full hover:bg-blue-700 transition-colors">
                        <i class="fas fa-search"></i>
                    </button>
                </div>
            </form>
        </div>
    </header>

    <!-- 메인 콘텐츠 -->
    <main class="container mx-auto max-w-4xl px-8 py-8 flex-grow">
        <!-- 검색 정보 및 인기 차트 바로가기 -->
        <div class="flex flex-col md:flex-row md:items-end justify-between gap-4 mb-8">
            <!-- 좌측: 검색 결과 타이틀 -->
            <div>
                <h1 class="text-2xl md:text-3xl font-bold mb-2 text-gray-800">
                    <% if (searchQuery && searchQuery.trim()) { %>
                        "<span class="text-blue-600"><%= searchQuery %></span>" 검색 결과
                    <% } else { %>
                        전체 노래 목록
                    <% } %>
                </h1>
                <div class="flex items-center gap-4">
                    <p class="text-gray-500 text-sm">
                        총 <span class="font-bold text-gray-700"><%= totalResults %></span>개의 결과를 찾았습니다.
                    </p>
                    
                    <!-- 정렬 옵션 -->
                    <div class="flex items-center gap-2 text-sm">
                        <span class="text-gray-400">|</span>
                        <a href="/songs?q=<%= encodeURIComponent(searchQuery) %>&sort=popular" 
                           class="<%= (!locals.currentSort || currentSort === 'popular') ? 'font-bold text-blue-600' : 'text-gray-500 hover:text-gray-700' %>">
                            인기순
                        </a>
                        <span class="text-gray-300">·</span>
                        <a href="/songs?q=<%= encodeURIComponent(searchQuery) %>&sort=latest" 
                           class="<%= (locals.currentSort === 'latest') ? 'font-bold text-blue-600' : 'text-gray-500 hover:text-gray-700' %>">
                            최신순
                        </a>
                    </div>
                </div>
            </div>

            <!-- 우측: 인기 차트 바로가기 버튼 -->
            <div class="flex gap-2">
                <a href="/popular/songs" class="inline-flex items-center px-4 py-2 bg-white border border-gray-200 rounded-full text-sm font-medium text-gray-700 hover:border-blue-300 hover:text-blue-600 hover:bg-blue-50 transition-all shadow-sm group">
                    <i class="fas fa-fire mr-2 text-red-500"></i>
                    인기 노래
                </a>
                <a href="/popular/artists" class="inline-flex items-center px-4 py-2 bg-white border border-gray-200 rounded-full text-sm font-medium text-gray-700 hover:border-purple-300 hover:text-purple-600 hover:bg-purple-50 transition-all shadow-sm group">
                    <i class="fas fa-crown mr-2 text-yellow-500"></i>
                    인기 아티스트
                </a>
            </div>
        </div>

        <!-- 검색 결과가 0개일 때 노래 추가 권유 섹션 -->
        <% if (totalResults === 0 && searchQuery && searchQuery.trim()) { %>
            <div class="text-center py-16">
                <div class="max-w-md mx-auto">
                    <div class="text-6xl text-gray-300 mb-6">
                        <i class="fas fa-search"></i>
                    </div>
                    <h3 class="text-xl font-semibold text-gray-700 mb-3">
                        "<span class="text-blue-600"><%= searchQuery %></span>"에 대한 검색 결과가 없습니다
                    </h3>
                    <p class="text-gray-500 mb-8 leading-relaxed">
                        찾으시는 노래가 없나요?<br>
                        직접 노래를 추가해서 Haneum 커뮤니티에 기여해보세요!
                    </p>
                    
                    <!-- 노래 추가 권유 버튼들 -->
                    <div class="space-y-4">
                        <a href="/add-song" 
                           class="inline-flex items-center justify-center w-full px-6 py-4 bg-blue-600 text-white font-semibold rounded-xl hover:bg-blue-700 transition-all duration-300 shadow-md hover:shadow-lg">
                            <i class="fas fa-plus mr-3"></i>
                            노래 추가하기
                        </a>
                        
                        <div class="text-sm text-gray-400 my-4">또는</div>
                        
                        <div class="grid grid-cols-1 sm:grid-cols-2 gap-3">
                            <a href="/songs" 
                               class="inline-flex items-center justify-center px-4 py-3 bg-gray-100 text-gray-700 font-medium rounded-lg hover:bg-gray-200 transition-colors">
                                <i class="fas fa-list mr-2"></i>
                                전체 노래 보기
                            </a>
                            <a href="/" 
                               class="inline-flex items-center justify-center px-4 py-3 bg-indigo-100 text-indigo-700 font-medium rounded-lg hover:bg-indigo-200 transition-colors">
                                <i class="fas fa-home mr-2"></i>
                                홈으로 돌아가기
                            </a>
                        </div>
                    </div>
                    
                    <!-- 추가 팁 -->
                    <div class="mt-8 p-4 bg-blue-50 rounded-lg text-left">
                        <h4 class="font-semibold text-blue-800 mb-2 flex items-center">
                            <i class="fas fa-lightbulb mr-2"></i>
                            검색 팁
                        </h4>
                        <ul class="text-sm text-blue-700 space-y-1">
                            <li>• 다른 언어로 된 제목으로 검색해보세요</li>
                            <li>• 아티스트 이름으로 검색해보세요</li>
                            <li>• 띄어쓰기를 줄이거나 늘려보세요</li>
                        </ul>
                    </div>
                </div>
            </div>
        <% } else if (totalResults === 0) { %>
            <!-- 전체 검색에서 결과가 없을 때 -->
            <div class="text-center py-16">
                <div class="text-6xl text-gray-300 mb-6">
                    <i class="fas fa-music"></i>
                </div>
                <h3 class="text-xl font-semibold text-gray-700 mb-3">
                    아직 등록된 노래가 없습니다
                </h3>
                <p class="text-gray-500 mb-8">
                    첫 번째 노래를 추가해보세요!
                </p>
                <a href="/add-song" 
                   class="inline-flex items-center justify-center px-8 py-4 bg-blue-600 text-white font-semibold rounded-xl hover:bg-blue-700 transition-all duration-300 shadow-md hover:shadow-lg">
                    <i class="fas fa-plus mr-3"></i>
                    노래 추가하기
                </a>
            </div>
        <% } else { %>
            <!-- 결과 목록 -->
            <div class="space-y-4">
                <% songs.forEach(song => { %>
                        <div class="bg-white p-6 rounded-lg shadow hover:shadow-md transition-shadow cursor-pointer" onclick="location.href='/songs/<%= encodeURIComponent(song.name) %>'">
                            <div class="flex justify-between items-start">
                                <div class="flex-grow">
                                    <h2 class="text-xl font-semibold mb-1 hover:text-blue-600">
                                        <%= song.ori_name || song.name %>
                                    </h2>
                                    <%
                                    const mainName = song.ori_name || song.name;
                                    const subNames = [];
                                    if (song.kor_name) {
                                        subNames.push(song.kor_name);
                                    }
                                    if (song.eng_name && song.eng_name !== mainName) {
                                        subNames.push(song.eng_name);
                                    }
                                    
                                    if (subNames.length > 0) {
                                    %>
                                        <p class="text-gray-500 text-sm mb-2"><%= subNames.join(' / ') %></p>
                                    <%
                                    }
                                    %>
                                    
                                    <% 
                                    let artistDisplayName;
                                    let artistSearchQuery;
                                    if (typeof song.artist === 'object' && song.artist) {
                                        const names = [];
                                        if (song.artist.ori_name) {
                                            names.push(song.artist.ori_name);
                                        }
                                        if (song.artist.kor_name && !names.includes(song.artist.kor_name)) {
                                            names.push(song.artist.kor_name);
                                        }
                                        if (song.artist.eng_name && !names.includes(song.artist.eng_name)) {
                                            names.push(song.artist.eng_name);
                                        }
                                        artistDisplayName = names.join(' / ');
                                        artistSearchQuery = song.artist.ori_name || artistDisplayName;
                                    } else {
                                        artistDisplayName = song.artist;
                                        artistSearchQuery = song.artist;
                                    }
                                    %>
                                    <p class="mt-2 text-blue-600 text-sm">
                                        <a href="/songs?q=<%= encodeURIComponent(artistSearchQuery) %>" onclick="event.stopPropagation()" class="hover:underline transition-colors"><%= artistDisplayName %></a>
                                    </p>
                                    <% if (song.tags && song.tags.length > 0) { %>
                                        <div class="flex flex-wrap gap-2 mt-3">
                                            <% song.tags.forEach(tag => { %>
                                                <a href="/songs?q=<%= encodeURIComponent(tag) %>" onclick="event.stopPropagation()" class="bg-blue-50 text-blue-600 text-xs font-semibold px-2 py-0.5 rounded-full hover:bg-blue-200 transition-colors"><%= tag %></a>
                                            <% }); %>
                                        </div>
                                    <% } %>
                                    <% if (song.createdAt) { %>
                                    <p class="text-xs text-gray-400 mt-2">
                                        <%= new Date(song.createdAt).toLocaleDateString('ko-KR', {
                                            year: 'numeric',
                                            month: 'long',
                                            day: 'numeric'
                                        }) %>
                                    </p>
                                    <% } %>
                                </div>
                            </div>
                        </div>
                <% }); %>
            </div>
        <% } %>

        <!-- 페이지네이션 -->
        <% if (totalPages > 1) { %>
            <div class="mt-8 flex justify-center space-x-2">
                <% if (currentPage > 1) { %>
                    <a href="/songs?q=<%= encodeURIComponent(searchQuery) %>&page=1&sort=<%= locals.currentSort || 'popular' %>" 
                       class="px-4 py-2 border rounded-md hover:bg-gray-100">
                        처음
                    </a>
                <% } %>
                
                <% 
                let startPage = Math.max(1, currentPage - 2);
                let endPage = Math.min(totalPages, startPage + 4);
                
                if (endPage - startPage < 4) {
                    startPage = Math.max(1, endPage - 4);
                }
                
                for (let i = startPage; i <= endPage; i++) { 
                %>
                    <a href="/songs?q=<%= encodeURIComponent(searchQuery) %>&page=<%= i %>&sort=<%= locals.currentSort || 'popular' %>" 
                       class="px-4 py-2 border rounded-md <%= i === currentPage ? 'bg-blue-600 text-white' : 'hover:bg-gray-100' %>">
                        <%= i %>
                    </a>
                <% } %>

                <% if (currentPage < totalPages) { %>
                    <a href="/songs?q=<%= encodeURIComponent(searchQuery) %>&page=<%= totalPages %>&sort=<%= locals.currentSort || 'popular' %>" 
                       class="px-4 py-2 border rounded-md hover:bg-gray-100">
                        마지막
                    </a>
                <% } %>
            </div>
        <% } %>
    </main>

    <!-- 푸터 -->
    <footer class="bg-gray-800 text-gray-400 py-8 px-6 text-center mt-12">
        <div class="container mx-auto max-w-6xl">
            <!-- <nav class="mb-6">
                <ul class="flex flex-wrap justify-center gap-x-6 gap-y-2">
                    <li><a href="/about" class="hover:text-white transition duration-200">About Haneum</a></li>
                    <li><a href="/contact" class="hover:text-white transition duration-200">문의하기</a></li>
                    <li><a href="/privacy" class="hover:text-white transition duration-200">개인정보처리방침</a></li>
                    <li><a href="/terms" class="hover:text-white transition duration-200">이용약관</a></li>
                    <li><a href="/copyright" class="hover:text-white transition duration-200">저작권 안내</a></li>
                </ul>
            </nav> -->
            <p class="notice text-xs text-gray-500 mt-2 max-w-xl mx-auto">본 사이트의 가사 정보는 교육 및 비상업적 용도로 제공되며, 저작권은 해당 저작권자에게 있습니다.</p>
        </div>
    </footer>
    <script>
        document.addEventListener('DOMContentLoaded', () => {
            const searchForm = document.getElementById('searchForm');
            const searchInput = document.getElementById('searchInput');

            if (searchForm && searchInput) {
                searchForm.addEventListener('submit', () => {
                    searchInput.value = searchInput.value.trim();
                });
            }
        });
    </script>
</body>
</html> 