<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>유저 관리 - 오픈 한음</title>
    <link rel="apple-touch-icon" sizes="180x180" href="/favicon/apple-touch-icon.png">
    <link rel="icon" type="image/png" sizes="32x32" href="/favicon/favicon-32x32.png">
    <link rel="icon" type="image/png" sizes="16x16" href="/favicon/favicon-16x16.png">
    <link rel="manifest" href="/favicon/site.webmanifest">
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css">
</head>
<body class="bg-gray-100 text-gray-800 font-sans leading-relaxed">
    <%- include('partials/header') %>
    
    <div class="bg-red-600 text-white text-center py-8 px-6">
        <div class="container mx-auto max-w-6xl">
            <h1 class="text-3xl md:text-4xl font-bold mb-4">유저 관리</h1>
            <p class="text-red-100">모든 가입 유저 정보를 조회합니다.</p>
            
            <div class="flex justify-center gap-4 mt-6">
                <a href="/admin" class="bg-white text-red-600 font-semibold px-6 py-3 rounded-full transition duration-300 ease-in-out inline-flex items-center justify-center gap-2">
                    <i class="fas fa-arrow-left"></i> 관리 메인으로
                </a>
            </div>
        </div>
    </div>

    <main class="py-8 px-6">
        <div class="container mx-auto max-w-6xl">
            <div class="bg-white rounded-lg shadow-sm overflow-hidden">
                <div class="p-6 border-b border-gray-200 flex justify-between items-center">
                    <h2 class="text-xl font-semibold">유저 목록 (<%= users.length %>명)</h2>
                </div>
                
                <div class="overflow-x-auto">
                    <table class="w-full text-left border-collapse">
                        <thead>
                            <tr class="bg-gray-50 border-b border-gray-200">
                                <th class="px-6 py-4 font-semibold text-gray-600">ID</th>
                                <th class="px-6 py-4 font-semibold text-gray-600">아이디</th>
                                <th class="px-6 py-4 font-semibold text-gray-600">가입일</th>
                            </tr>
                        </thead>
                        <tbody class="divide-y divide-gray-200">
                            <% users.forEach(user => { %>
                                <tr class="hover:bg-gray-50 transition duration-150">
                                    <td class="px-6 py-4 text-gray-500"><%= user.id %></td>
                                    <td class="px-6 py-4 font-medium text-gray-800"><%= user.username %></td>
                                    <td class="px-6 py-4 text-gray-600">
                                        <%= new Date(user.created_at).toLocaleString('ko-KR') %>
                                    </td>
                                </tr>
                            <% }); %>
                        </tbody>
                    </table>
                </div>
                
                <% if (users.length === 0) { %>
                    <div class="p-12 text-center text-gray-500">
                        가입된 유저가 없습니다.
                    </div>
                <% } %>
            </div>
        </div>
    <%- include('partials/footer') %>
</body>
</html>

